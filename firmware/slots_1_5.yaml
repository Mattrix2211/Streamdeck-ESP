# Entites des emplacements 1 a 5 (voir docs/ARCHITECTURE.md pour le
# systeme de 16 emplacements configurables). Genere, ne pas editer a la main.
text:
  - platform: template
    id: slot1_label
    name: "Slot 1 - libelle"
    mode: text
    optimistic: true
    initial_value: "Slot 1"
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot1_title, text: !lambda "return x;" }
  - platform: template
    id: slot1_value
    name: "Slot 1 - valeur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot1_value_lbl, text: !lambda "return x;" }
      - lambda: |-
          char *endptr;
          long v = strtol(x.c_str(), &endptr, 10);
          if (endptr != x.c_str()) {
            if (v < 0) v = 0;
            if (v > 100) v = 100;
            lv_bar_set_value(id(slot1_bar), (int) v, LV_ANIM_OFF);
          }
  - platform: template
    id: slot1_icon
    name: "Slot 1 - icone"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 8
    on_value:
      - lvgl.label.update: { id: slot1_icon_lbl, text: !lambda "return x;" }
  - platform: template
    id: slot1_color
    name: "Slot 1 - couleur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 7
    on_value:
      - lvgl.obj.update: {id: slot1_btn, bg_color: !lambda 'return x.length() == 7 ? Color(strtoul(x.substr(1,2).c_str(), nullptr, 16), strtoul(x.substr(3,2).c_str(), nullptr, 16), strtoul(x.substr(5,2).c_str(), nullptr, 16)) : Color(0x0F, 0x29, 0x42);'}
  - platform: template
    id: slot2_label
    name: "Slot 2 - libelle"
    mode: text
    optimistic: true
    initial_value: "Slot 2"
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot2_title, text: !lambda "return x;" }
  - platform: template
    id: slot2_value
    name: "Slot 2 - valeur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot2_value_lbl, text: !lambda "return x;" }
      - lambda: |-
          char *endptr;
          long v = strtol(x.c_str(), &endptr, 10);
          if (endptr != x.c_str()) {
            if (v < 0) v = 0;
            if (v > 100) v = 100;
            lv_bar_set_value(id(slot2_bar), (int) v, LV_ANIM_OFF);
          }
  - platform: template
    id: slot2_icon
    name: "Slot 2 - icone"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 8
    on_value:
      - lvgl.label.update: { id: slot2_icon_lbl, text: !lambda "return x;" }
  - platform: template
    id: slot2_color
    name: "Slot 2 - couleur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 7
    on_value:
      - lvgl.obj.update: {id: slot2_btn, bg_color: !lambda 'return x.length() == 7 ? Color(strtoul(x.substr(1,2).c_str(), nullptr, 16), strtoul(x.substr(3,2).c_str(), nullptr, 16), strtoul(x.substr(5,2).c_str(), nullptr, 16)) : Color(0x0F, 0x29, 0x42);'}
  - platform: template
    id: slot3_label
    name: "Slot 3 - libelle"
    mode: text
    optimistic: true
    initial_value: "Slot 3"
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot3_title, text: !lambda "return x;" }
  - platform: template
    id: slot3_value
    name: "Slot 3 - valeur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot3_value_lbl, text: !lambda "return x;" }
      - lambda: |-
          char *endptr;
          long v = strtol(x.c_str(), &endptr, 10);
          if (endptr != x.c_str()) {
            if (v < 0) v = 0;
            if (v > 100) v = 100;
            lv_bar_set_value(id(slot3_bar), (int) v, LV_ANIM_OFF);
          }
  - platform: template
    id: slot3_icon
    name: "Slot 3 - icone"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 8
    on_value:
      - lvgl.label.update: { id: slot3_icon_lbl, text: !lambda "return x;" }
  - platform: template
    id: slot3_color
    name: "Slot 3 - couleur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 7
    on_value:
      - lvgl.obj.update: {id: slot3_btn, bg_color: !lambda 'return x.length() == 7 ? Color(strtoul(x.substr(1,2).c_str(), nullptr, 16), strtoul(x.substr(3,2).c_str(), nullptr, 16), strtoul(x.substr(5,2).c_str(), nullptr, 16)) : Color(0x0F, 0x29, 0x42);'}
  - platform: template
    id: slot4_label
    name: "Slot 4 - libelle"
    mode: text
    optimistic: true
    initial_value: "Slot 4"
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot4_title, text: !lambda "return x;" }
  - platform: template
    id: slot4_value
    name: "Slot 4 - valeur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot4_value_lbl, text: !lambda "return x;" }
      - lambda: |-
          char *endptr;
          long v = strtol(x.c_str(), &endptr, 10);
          if (endptr != x.c_str()) {
            if (v < 0) v = 0;
            if (v > 100) v = 100;
            lv_bar_set_value(id(slot4_bar), (int) v, LV_ANIM_OFF);
          }
  - platform: template
    id: slot4_icon
    name: "Slot 4 - icone"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 8
    on_value:
      - lvgl.label.update: { id: slot4_icon_lbl, text: !lambda "return x;" }
  - platform: template
    id: slot4_color
    name: "Slot 4 - couleur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 7
    on_value:
      - lvgl.obj.update: {id: slot4_btn, bg_color: !lambda 'return x.length() == 7 ? Color(strtoul(x.substr(1,2).c_str(), nullptr, 16), strtoul(x.substr(3,2).c_str(), nullptr, 16), strtoul(x.substr(5,2).c_str(), nullptr, 16)) : Color(0x0F, 0x29, 0x42);'}
  - platform: template
    id: slot5_label
    name: "Slot 5 - libelle"
    mode: text
    optimistic: true
    initial_value: "Slot 5"
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot5_title, text: !lambda "return x;" }
  - platform: template
    id: slot5_value
    name: "Slot 5 - valeur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 24
    on_value:
      - lvgl.label.update: { id: slot5_value_lbl, text: !lambda "return x;" }
      - lambda: |-
          char *endptr;
          long v = strtol(x.c_str(), &endptr, 10);
          if (endptr != x.c_str()) {
            if (v < 0) v = 0;
            if (v > 100) v = 100;
            lv_bar_set_value(id(slot5_bar), (int) v, LV_ANIM_OFF);
          }
  - platform: template
    id: slot5_icon
    name: "Slot 5 - icone"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 8
    on_value:
      - lvgl.label.update: { id: slot5_icon_lbl, text: !lambda "return x;" }
  - platform: template
    id: slot5_color
    name: "Slot 5 - couleur"
    mode: text
    optimistic: true
    initial_value: ""
    max_length: 7
    on_value:
      - lvgl.obj.update: {id: slot5_btn, bg_color: !lambda 'return x.length() == 7 ? Color(strtoul(x.substr(1,2).c_str(), nullptr, 16), strtoul(x.substr(3,2).c_str(), nullptr, 16), strtoul(x.substr(5,2).c_str(), nullptr, 16)) : Color(0x0F, 0x29, 0x42);'}

select:
  - platform: template
    id: slot1_type
    name: "Slot 1 - type"
    optimistic: true
    options: ["bouton", "barre", "texte"]
    initial_option: "bouton"
    on_value:
      - lambda: |-
          if (x == "barre") {
            lv_obj_clear_flag(id(slot1_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot1_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else if (x == "texte") {
            lv_obj_add_flag(id(slot1_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(slot1_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot1_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot1_value_lbl), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot2_type
    name: "Slot 2 - type"
    optimistic: true
    options: ["bouton", "barre", "texte"]
    initial_option: "bouton"
    on_value:
      - lambda: |-
          if (x == "barre") {
            lv_obj_clear_flag(id(slot2_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot2_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else if (x == "texte") {
            lv_obj_add_flag(id(slot2_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(slot2_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot2_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot2_value_lbl), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot3_type
    name: "Slot 3 - type"
    optimistic: true
    options: ["bouton", "barre", "texte"]
    initial_option: "bouton"
    on_value:
      - lambda: |-
          if (x == "barre") {
            lv_obj_clear_flag(id(slot3_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot3_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else if (x == "texte") {
            lv_obj_add_flag(id(slot3_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(slot3_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot3_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot3_value_lbl), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot4_type
    name: "Slot 4 - type"
    optimistic: true
    options: ["bouton", "barre", "texte"]
    initial_option: "bouton"
    on_value:
      - lambda: |-
          if (x == "barre") {
            lv_obj_clear_flag(id(slot4_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot4_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else if (x == "texte") {
            lv_obj_add_flag(id(slot4_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(slot4_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot4_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot4_value_lbl), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot5_type
    name: "Slot 5 - type"
    optimistic: true
    options: ["bouton", "barre", "texte"]
    initial_option: "bouton"
    on_value:
      - lambda: |-
          if (x == "barre") {
            lv_obj_clear_flag(id(slot5_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot5_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else if (x == "texte") {
            lv_obj_add_flag(id(slot5_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(slot5_value_lbl), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot5_bar), LV_OBJ_FLAG_HIDDEN);
            lv_obj_add_flag(id(slot5_value_lbl), LV_OBJ_FLAG_HIDDEN);
          }

switch:
  - platform: template
    id: slot1_visible
    name: "Slot 1 - visible"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_ON
    on_state:
      - lambda: |-
          if (x) {
            lv_obj_clear_flag(id(slot1_btn), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot1_btn), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot2_visible
    name: "Slot 2 - visible"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_ON
    on_state:
      - lambda: |-
          if (x) {
            lv_obj_clear_flag(id(slot2_btn), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot2_btn), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot3_visible
    name: "Slot 3 - visible"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_ON
    on_state:
      - lambda: |-
          if (x) {
            lv_obj_clear_flag(id(slot3_btn), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot3_btn), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot4_visible
    name: "Slot 4 - visible"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_ON
    on_state:
      - lambda: |-
          if (x) {
            lv_obj_clear_flag(id(slot4_btn), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot4_btn), LV_OBJ_FLAG_HIDDEN);
          }
  - platform: template
    id: slot5_visible
    name: "Slot 5 - visible"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_ON
    on_state:
      - lambda: |-
          if (x) {
            lv_obj_clear_flag(id(slot5_btn), LV_OBJ_FLAG_HIDDEN);
          } else {
            lv_obj_add_flag(id(slot5_btn), LV_OBJ_FLAG_HIDDEN);
          }
